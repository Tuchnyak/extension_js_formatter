# Спецификация: офлайн JSON Viewer — Chrome-расширение

Рабочее название: `<EXTENSION_NAME>` (финальное название выберет владелец проекта; до этого используй плейсхолдер в `wxt.config.ts`).

---

## 0. Правила работы для агента (читать первым)

1. **Работаем строго по шагам.** Один шаг — одна сессия работы. После завершения шага **остановись** и выведи:
   - что сделано (кратко);
   - результат `npm run compile` и `npx biome check .`;
   - чек-лист ручной проверки из раздела «Проверка» этого шага.
   Не начинай следующий шаг без явного подтверждения владельца.
2. **Каждый шаг достраивает предыдущий, а не переписывает его.** Если для шага нужно существенно менять код прошлых шагов — остановись и объясни почему, прежде чем менять.
3. **Если спецификация расходится с реальностью** (API WXT/Chrome работает не так, как описано; команда не существует; решение требует нового permission) — остановись и спроси. Не импровизируй молча.
4. **Сверяйся с актуальной документацией WXT (wxt.dev).** API менялся между версиями: например, в 0.20.x удалён экспорт `wxt/sandbox`. Не используй импорты из старых туториалов; `defineBackground`, `defineContentScript`, `browser` в WXT доступны через auto-imports.
5. **Коммит в конце каждого шага** с сообщением вида `v1 step 3: collapsible tree and themes`.
6. **Язык:** весь UI — на английском. Комментарии в коде — на английском. Отчёты владельцу — на русском.

---

## 1. Продукт и жёсткие принципы

**Что это:** расширение, которое красиво и безопасно показывает JSON: автоматически на JSON-страницах, из вставленного вручную текста и из выделенного на любой странице фрагмента.

**Непреложные ограничения (нарушение = баг):**

- **Ноль сетевых запросов.** Никаких `fetch`, аналитики, телеметрии, шрифтов/скриптов с CDN, remote config. Всё работает офлайн.
- **Ноль runtime-зависимостей.** Парсинг — нативный `JSON.parse`, рендер и подсветка — собственный код. Dev-зависимости (WXT, TypeScript, Biome, позже Vitest) допустимы.
- **Минимальные permissions:** только `contextMenus`, `storage` и host-доступ для content script (см. раздел 3.4). Любой новый permission — только после согласования.
- **Никакого `innerHTML` с данными пользователя.** DOM строится через `createElement` / `textContent`. Ссылки — только схемы `http:` / `https:`, с `rel="noopener noreferrer"` и `target="_blank"`.
- **Никаких аккаунтов, никакого хранения пользовательских данных** кроме настроек (тема) и временной передачи выделенного текста.

---

## 2. Стек

| Слой | Инструмент | Примечание |
|---|---|---|
| Рантайм | Node.js LTS | Уже установлен у владельца или ставится через fnm/nvm |
| Пакетный менеджер | npm | |
| Фреймворк расширения | WXT, шаблон **vanilla + TypeScript** | Зафиксировать версию патч-диапазоном (`~0.20.x`), не `^` |
| Линт/формат | Biome | Один конфиг `biome.json` в корне |
| Тесты | Vitest | **Только начиная с релиза v2** |
| UI-фреймворк | **нет** | Никаких React/Vue/Svelte |

Скрипты в `package.json` (минимум):

```json
{
  "scripts": {
    "dev": "wxt",
    "build": "wxt build",
    "zip": "wxt zip",
    "compile": "tsc --noEmit",
    "check": "biome check ."
  }
}
```

---

## 3. Архитектура

### 3.1. Структура проекта

```
entrypoints/
  background.ts        # context menu, click on toolbar icon
  content.ts           # auto-format JSON documents
  viewer.html          # unlisted page: viewer + manual paste
  viewer/main.ts       # viewer page logic (or inline module per WXT conventions)
lib/
  parse.ts             # JSON.parse wrapper + error location (line/column)
  render.ts            # value -> DOM tree (shared by viewer and content script)
  toolbar.ts           # raw/parsed toggle, expand/collapse all, theme switch
  theme.ts             # theme state: system | light | dark, persisted
  styles.css           # shared styles, CSS custom properties, all classes prefixed `jv-`
fixtures/              # sample JSON files for manual testing
store/                 # store listing drafts (step 6)
public/icon/           # extension icons
```

Если конвенции WXT требуют иного расположения файлов (например, `entrypoints/viewer/index.html`) — следуй WXT, сохраняя разделение ответственности.

### 3.2. Общее ядро

**Ключевой принцип: один движок рендера, три точки входа.** `lib/parse.ts` + `lib/render.ts` + `lib/toolbar.ts` + `lib/styles.css` используются и в viewer-странице, и в content script без дублирования.

`lib/parse.ts`:
- `parseJson(text: string): { ok: true; value: unknown } | { ok: false; message: string; line?: number; column?: number }`.
- Позицию ошибки вычислять **самостоятельно** из смещения в сообщении `SyntaxError` (формат сообщений V8 может меняться — извлечь `position N`, если есть, и пересчитать в line/column по исходному тексту). Если позиция не найдена — вернуть только `message`.

`lib/render.ts`:
- `renderJson(value: unknown): HTMLElement` — рекурсивный обход.
- Константа `MAX_RENDER_BYTES = 10 * 1024 * 1024`. Если исходный текст больше — дерево **не строить**, показать raw-текст и уведомление «Too large to format (N MB). Showing raw.»

### 3.3. Три точки входа

1. **Прямая навигация на JSON-URL** → `content.ts` подменяет содержимое страницы отформатированным деревом.
2. **Клик по иконке расширения** → `background.ts` открывает `viewer.html` в новой вкладке (без popup: popup закрывается при потере фокуса, для просмотра неудобно). Для работы `action.onClicked` в манифест нужно явно добавить `action: {}` через `wxt.config.ts`.
3. **Выделение + контекстное меню** → `background.ts` кладёт текст в `storage.session` и открывает `viewer.html?id=<key>`; viewer читает значение, **сразу удаляет его** из storage и форматирует.

### 3.4. Permissions (итоговые для v1)

- `contextMenus` — пункт меню на выделенном тексте.
- `storage` — `storage.session` для передачи выделения; `storage.local` для темы.
- Host-доступ для content script: `matches: ["<all_urls>"]`. **Это неизбежно** для автоформата на любом JSON-URL, и Chrome покажет пользователю предупреждение «Read and change all your data on all websites». Компенсация — content script мгновенно выходит на не-JSON страницах и ничего не читает/не отправляет (см. шаг 5), и это явно пишется в описании стора.

---

## 4. Релиз v1.0.0 — MVP

### Шаг 1 — Скелет проекта

**Сделать:**
- Инициализировать проект WXT (vanilla + TypeScript), npm.
- Добавить Biome с конфигом по умолчанию + скрипты из раздела 2.
- `wxt.config.ts`: `name`, `description` (плейсхолдеры), `action: {}`.
- `background.ts`: по клику на иконку открыть `viewer.html` в новой вкладке.
- `viewer.html`: заголовок `<EXTENSION_NAME>` и текст-заглушка «Viewer works».
- Создать `fixtures/` с файлами:
  - `simple.json` — плоский объект со строкой, числом, bool, null;
  - `nested.json` — 4–5 уровней вложенности, массивы объектов;
  - `edge-cases.json` — пустые `{}` и `[]`, unicode (кириллица, эмодзи), строки с экранированием `\n`, `\"`, `\\`, число больше `Number.MAX_SAFE_INTEGER`, URL-строки;
  - `invalid-trailing-comma.json`, `invalid-unclosed.json` — заведомо битые;
  - скрипт `fixtures/generate-large.mjs`, генерирующий `large.json` ~5 МБ (сам `large.json` — в `.gitignore`).

**Проверка:**
- [ ] `npm run dev` открывает Chrome с загруженным расширением.
- [ ] Клик по иконке открывает новую вкладку с заглушкой.
- [ ] `npm run compile` и `npm run check` — без ошибок.

### Шаг 2 — Движок: парсинг и плоский рендер (вход: ручная вставка)

**Сделать:**
- `lib/parse.ts` по разделу 3.2.
- `lib/render.ts`: рекурсивный рендер **без схлопывания**. Каждому токену CSS-класс по типу: `jv-key`, `jv-string`, `jv-number`, `jv-boolean`, `jv-null`, `jv-punct`. Строки отображаются в кавычках с экранированием как в JSON (использовать `JSON.stringify(str)` для отображения).
- Viewer: `<textarea>` + кнопка **Format** + горячая клавиша Ctrl/Cmd+Enter. Под ними — область результата.
- При ошибке парсинга: сообщение, `line:column`, строка исходника с этой позицией и маркер `^` под нужным символом. Никакого белого экрана.

**Проверка:**
- [ ] `simple.json`, `nested.json`, `edge-cases.json` отображаются раскрашенными, с отступами.
- [ ] Unicode и экранированные символы отображаются корректно.
- [ ] Оба invalid-файла показывают понятную ошибку с номером строки/столбца.
- [ ] Пустой ввод + Format — понятное сообщение, а не падение.

### Шаг 3 — Визуальные возможности

**Сделать:**
- Объекты и массивы — через нативные `<details open>` / `<summary>`. В свёрнутом состоянии summary показывает `{…}` / `[…]` и счётчик: `3 items`.
- Пустые `{}` и `[]` — inline, без `<details>`.
- Indent guides (вертикальные линии уровней вложенности) через CSS.
- Строковые значения, целиком являющиеся `http(s)://` URL, — кликабельные ссылки (правила из раздела 1).
- `lib/toolbar.ts`: кнопки **Raw / Parsed** (Raw = исходный текст как есть), **Expand all**, **Collapse all**, переключатель темы **System / Light / Dark**.
- `lib/theme.ts`: тема через CSS custom properties на корне; значение по умолчанию System (`prefers-color-scheme`); выбор сохраняется в `storage.local`.
- Проверка размера: при превышении `MAX_RENDER_BYTES` — raw + уведомление (раздел 3.2).

**Проверка:**
- [ ] Узлы сворачиваются/разворачиваются кликом и с клавиатуры (Tab + Enter/Space).
- [ ] Expand all / Collapse all работают на `nested.json`.
- [ ] Тема переключается и сохраняется после перезагрузки вкладки.
- [ ] URL в `edge-cases.json` кликабельны и открываются в новой вкладке.
- [ ] `large.json` (~5 МБ) рендерится; вкладка не зависает намертво (кратковременная задержка допустима — зафиксировать время в отчёте).

### Шаг 4 — Вход «выделение → контекстное меню»

**Сделать:**
- В `background.ts`: создать пункт меню в `runtime.onInstalled` (MV3: service worker не живёт постоянно), контекст `selection`, заголовок `Format selected JSON`.
- По клику: сгенерировать ключ, `storage.session.set({ [key]: info.selectionText })`, открыть `viewer.html?id=<key>`.
- Viewer: при наличии `id` — прочитать текст, удалить ключ из storage, подставить в textarea и сразу отформатировать.
- Если ключа нет (вкладку перезагрузили) — пустой viewer без ошибки.

**Проверка:**
- [ ] Выделить JSON на любой странице (например, в gist или в тексте чата) → правый клик → пункт меню → новая вкладка с отформатированным результатом.
- [ ] Выделить не-JSON текст → в новой вкладке понятная ошибка парсинга.
- [ ] После открытия ключ удалён из `storage.session` (проверить в DevTools расширения).
- [ ] **Выделить большой фрагмент** (например, половину `large.json`, открытого как текст) и проверить, не обрезается ли `selectionText`. Если обрезается — не чинить, а зафиксировать в отчёте как известное ограничение.

### Шаг 5 — Вход «прямая навигация на JSON-URL»

**Сделать:**
- `content.ts`, `matches: ["<all_urls>"]`, `run_at: "document_end"`.
- **Первая строка логики** — проверка `document.contentType`: `application/json`, `text/json` или тип, оканчивающийся на `+json`. Иначе — немедленный `return`, без любых других обращений к DOM.
- Взять исходный текст (содержимое `body` / первого `pre`, как его отдаёт Chrome), распарсить, **полностью заменить** содержимое `body` деревом + тулбаром (тот же `lib/`). Стили подключить через механизм CSS-инъекции WXT для content scripts.
- Если JSON битый — не трогать исходный текст, добавить сверху баннер с ошибкой.
- Если больше `MAX_RENDER_BYTES` — не трогать исходный текст, добавить баннер «Too large to format».

**Проверка:**
- [ ] `https://jsonplaceholder.typicode.com/users` — автоматически отформатирован.
- [ ] Локально: `npx serve fixtures` → открыть `http://localhost:3000/nested.json` — отформатирован.
- [ ] Обычные сайты (любые новостные, GitHub) — никаких изменений и ошибок в консоли.
- [ ] Raw/Parsed, тема, Expand/Collapse работают и здесь.
- [ ] Отметить в отчёте: работает ли на `file://` JSON при включённом «Allow access to file URLs» (не чинить, просто зафиксировать).

### Шаг 6 — Упаковка и материалы для стора

**Сделать:**
- Иконки 16/32/48/128 в `public/icon/`. Если владелец не предоставил свою — сделать простую нейтральную заглушку и явно пометить это в отчёте.
- Проверить итоговый `manifest.json` в `.output/chrome-mv3/`: permissions ровно те, что в разделе 3.4.
- `npm run zip`.
- Черновики в `store/` (на английском):
  - `description.md` — описание для листинга; акцент: offline, no tracking, no network requests, open source;
  - `single-purpose.md` — формулировка единственной цели расширения;
  - `permissions-justification.md` — обоснование каждого permission, отдельно — зачем host-доступ ко всем сайтам;
  - `privacy.md` — «расширение не собирает и не передаёт никаких данных».
- Версия в `package.json`: `1.0.0`.

**Проверка:**
- [ ] Удалить dev-версию из Chrome, распаковать zip и загрузить через Load unpacked — всё из шагов 2–5 работает так же.
- [ ] В `manifest.json` нет лишних permissions.
- [ ] Черновики в `store/` прочитаны владельцем.

---

## 5. Релиз v1.1.0 — удобства

### Шаг 1.1-1 — Copy path / Copy value

**Сделать:**
- При наведении на узел — две маленькие кнопки: **Copy path**, **Copy value**.
- Path: JS-нотация от корня — `users[3].address.city`; ключи, не являющиеся идентификаторами, — в скобках: `data["my-key"]`.
- Value: примитив — как есть; объект/массив — `JSON.stringify(value, null, 2)`.
- Копирование: `navigator.clipboard.writeText`, при неудаче (например, на `http://` странице в content script) — fallback через временный `textarea` + `document.execCommand('copy')`. Короткое визуальное подтверждение «Copied».

**Проверка:**
- [ ] Path корректен для вложенных объектов, элементов массивов и ключей со спецсимволами.
- [ ] Копирование работает во viewer и на JSON-странице (http и https).

### Шаг 1.1-2 — Ctrl/Cmd+клик сворачивает соседей

**Сделать:** Ctrl/Cmd+клик по `summary` переводит все соседние узлы того же родителя в то же состояние (открыт/закрыт).

**Проверка:**
- [ ] На массиве объектов Ctrl+клик по одному элементу сворачивает все элементы массива.

### Шаг 1.1-3 — Декодирование JWT

**Сделать:**
- Строка считается JWT-кандидатом, если соответствует `^[A-Za-z0-9_-]+\.[A-Za-z0-9_-]+\.[A-Za-z0-9_-]*$` **и** header декодируется (base64url → с корректировкой `-`/`_` и паддинга) в JSON-объект с полем `alg`.
- Рядом со строкой — переключатель **JWT**; раскрывает декодированные header и payload, отрендеренные тем же `renderJson`.
- Для полей `exp`, `iat`, `nbf` — рядом человекочитаемая дата (UTC) и пометка, истёк ли `exp`.
- Явная подпись: «Signature not verified».
- Любая ошибка декодирования — строка остаётся обычной строкой, без падения рендера.

**Проверка:**
- [ ] Добавить `fixtures/jwt.json` с валидным тестовым токеном и с «похожей на JWT» строкой — первый декодируется, вторая нет.

**В конце релиза:** версия `1.1.0`, `npm run zip`.

---

## 6. Релиз v2.0.0 — JSON → SQL INSERT (generic)

Без привязки к конкретной СУБД. Генерация стандартного SQL.

### Шаг 2-1 — Модуль конвертации + тесты

**Сделать:**
- Подключить Vitest (`npm run test`).
- `lib/sql.ts`: чистая функция `jsonToInsert(value, options): { ok: true; sql: string } | { ok: false; reason: string }`.
- Опции: `tableName: string`, `mode: "multi-row" | "per-row"`.
- Правила:
  - Вход: массив объектов (или один объект = одна строка). Иное — `ok: false` с понятной причиной.
  - Колонки: объединение ключей всех строк в порядке первого появления; отсутствующий ключ → `NULL`.
  - Строка → `'...'`, одинарные кавычки удваиваются.
  - Число → как есть. Известное ограничение: числа за пределами `Number.MAX_SAFE_INTEGER` уже потеряли точность при `JSON.parse` — задокументировать в UI.
  - `true`/`false` → `TRUE`/`FALSE`; `null` → `NULL`.
  - Вложенные объект/массив → JSON-текст как строковый литерал.
  - Идентификаторы (таблица, колонки): без кавычек, если соответствуют `^[A-Za-z_][A-Za-z0-9_]*$`, иначе в двойных кавычках с удвоением `"` внутри.
- Тесты покрывают каждое правило выше + пустой массив + массив примитивов (ошибка).

**Проверка:**
- [ ] `npm run test` — все тесты зелёные; владелец просматривает список тест-кейсов в отчёте.

### Шаг 2-2 — UI во viewer

**Сделать:**
- Во viewer — кнопка/вкладка **To SQL**: поле имени таблицы (по умолчанию `my_table`), выбор режима, область с результатом, кнопка **Copy**.
- Если корень не подходит (не объект и не массив объектов) — кнопка неактивна с подсказкой почему.
- На JSON-страницах (content script) — только кнопка **Open in Viewer**, передающая исходный текст во viewer тем же механизмом `storage.session`, что и контекстное меню.

**Проверка:**
- [ ] `nested.json` / массив объектов → корректный INSERT, который вставляется в любой SQL-клиент без синтаксических ошибок.
- [ ] Open in Viewer с JSON-страницы открывает viewer с тем же содержимым.

**В конце релиза:** версия `2.0.0`, `npm run zip`.

---

## 7. Бэклог (не делать без отдельного решения владельца)

- Поиск/фильтр по дереву с подсветкой и авто-раскрытием предков.
- Импорт/экспорт CSV и YAML.
- Парсинг в Web Worker и виртуализация рендера для файлов >10 МБ.
- Точное отображение больших чисел (кастомный парсер).
- Номера строк, соответствующие исходному тексту.
- Автоопределение JSON на страницах с `text/plain`.
- Локализация UI.
- Copy minified: удаление пробелов из исходного текста вне строк, без round-trip через `JSON.parse`/`stringify` (иначе теряется точность больших чисел и переставляются целочисленные ключи). Естественное место — рядом с Copy value (v1.1).
